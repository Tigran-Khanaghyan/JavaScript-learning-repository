"use strict";

//Would it be right to use upper case for birthday? For age? Or even for both?

//The birthday variable must be an upperCase mode, because we know the value of that constant before declaring.
const BIRTHDAY = "18.04.1982";

//The age variable must be a lowerCase mode, because we don`t know about ade`s value before declaring.
const age = someCode(birthday);

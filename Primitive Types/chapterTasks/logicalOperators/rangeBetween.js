"use strict";

//Write an if condition to check that age is
// between 14 and 90 inclusively.
//“Inclusively” means that age can reach the edges 14 or 90.

let age = 25;

if (age >= 14 && age <= 90) {
  console.log(true);
} else {
  console.log(false);
}
